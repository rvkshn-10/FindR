name: findr_flutter
description: Wayvio - find items nearby (stores, products, best option). Dart/Flutter version.
publish_to: 'none'
version: 1.5.8+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  geolocator: ^10.1.0
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  provider: ^6.1.0
  url_launcher: ^6.2.0
  shared_preferences: ^2.5.4
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true