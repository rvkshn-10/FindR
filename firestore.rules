rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Read/write disabled - app uses local storage only
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
