# Supply Map: optional. When set, AI ranks stores and summarizes the best option.
# OPENAI_API_KEY=sk-...

# Supply Map: optional. When set, store distances use Google Distance Matrix (road distance) instead of straight-line.
# Enable "Distance Matrix API" in Google Cloud Console.
# GOOGLE_MAPS_API_KEY=...

# Supply Map: optional. When set, the results map uses Google Maps instead of OpenStreetMap.
# Must be set in .env.local (client-side). Enable "Maps JavaScript API" in Google Cloud Console.
# Can be the same value as GOOGLE_MAPS_API_KEY if both APIs are enabled for that key.
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=...
